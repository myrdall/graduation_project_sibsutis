const SRV_PORT = 3000;
const SRV_HOST = 'localhost';

const DB_HOST = 'localhost';
const DB_USER = 'root';
const DB_PASSWORD = 'password';
const DB_NAME = 'mydatabase';

module.exports = {

    SRV_PORT,
    SRV_HOST,
    DB_HOST,
    DB_USER,
    DB_PASSWORD,
    DB_NAME
};